---
name: "POSN Mock Exam Builder"
description: "สร้างข้อสอบเทียม สอวน. วิชาคอมพิวเตอร์ (ค่าย 1) พร้อมเฉลย เป็น PDF ด้วย XeLaTeX ฟอนต์ TH Sarabun New ขนาดเท่าข้อสอบจริง แล้วอัปขึ้น Google Drive. Use when creating/editing POSN mock exams (ข้อสอบเทียม, ข้อสอบจำลอง สอวน.), generating Thai exam or answer-key PDFs, matching a real Thai exam's font and layout, or uploading files to this user's Google Drive."
---

# POSN Mock Exam Builder

สร้างข้อสอบเทียม สอวน. คอมพิวเตอร์ (คัดเลือกเข้าค่าย 1) ให้เหมือนข้อสอบจริงทุกด้าน — ฟอนต์
ขนาดตัวอักษร layout และการพิมพ์คณิตศาสตร์ — พร้อมเฉลยแยกไฟล์ และอัปขึ้น Google Drive

## กฎเหล็ก (ห้ามละเมิด)

1. **คณิตศาสตร์ทุกตัวต้องพิมพ์ด้วย LaTeX** ไม่มีข้อยกเว้น ห้ามใช้ Unicode math (²,√,≤,½)
   ห้ามใช้ plain text แทนสูตร ทุกอย่างอยู่ใน `$...$` หรือ `\[...\]`
2. **ฟอนต์ต้องเป็น TH Sarabun New** เท่านั้น (ไม่ใช่ Sarabun ของ Google ซึ่งเป็นคนละฟอนต์)
3. **ห้ามเดาคำตอบ** ทุกข้อต้องผ่านสคริปต์ Python brute-force ก่อนพิมพ์ลงไฟล์
4. **ต้อง cross-check ตารางเฉลยกับตัวเลือกจริง** ด้วย `scripts/check_answer_key.py` ก่อนส่งมอบ
5. ปกต้องระบุชัดว่าเป็นข้อสอบเทียมเพื่อฝึกฝน ไม่ใช่ข้อสอบจริงของมูลนิธิ สอวน.
6. **เฉลยต้องเลือกวิธีที่ง่ายและเห็นภาพที่สุดก่อน แล้วจึงแสดงวิธีนั้นให้ครบทุกขั้น**
   ทุกข้อครบ 5 ส่วน (แนวคิด / วิธีทำเป็นขั้น / ตอบ / ทำไมตัวเลือกอื่นผิด / จุดพลาด)
   ห้ามข้ามขั้นพีชคณิต ห้ามเขียนแค่ผลลัพธ์ และห้ามใช้เครื่องมือที่ซับซ้อนเกินจำเป็น ดูหัวข้อ 6
7. **โจทย์ต้องวัดการคิด ไม่ใช่ความถึก** ทุกข้อต้องมีไอเดียชี้ขาดอย่างน้อยหนึ่งอย่าง
   ห้ามออกข้อที่ชนะกันด้วยการไล่ค่าซ้ำ ๆ (หาคาบ ไล่ตารางยาว คำนวณเลขใหญ่ด้วยมือ)
   เกิน 2 ข้อต่อชุด ดูหัวข้อ 3

## Environment ที่ตั้งไว้แล้ว

| สิ่งของ | ตำแหน่ง |
|---|---|
| XeLaTeX (TinyTeX) | `~/.TinyTeX/bin/<arch>/` — ไม่มี LaTeX ระดับระบบ และไม่มี passwordless sudo |
| ฟอนต์ TH Sarabun New | `~/.local/share/fonts/thsarabunnew/` + `TEXMFHOME/fonts/truetype/public/thsarabunnew/` |
| rclone | `~/.local/bin/rclone` remote ชื่อ `gdrive` (authorized แล้ว) |
| ข้อสอบจริงอ้างอิง | `~/Downloads/POSN.Computer/ไฟล์ข้อสอบ/ข้อสอบสอวนคอมปี 64-68.pdf` (แยกไฟล์รายปี) |
| ขอบเขตเนื้อหา | `~/Downloads/POSN.Computer/ไฟล์ข้อสอบ/เนื้อหาที่ใช้สอบ.pdf` |
| ไฟล์ที่ทำเสร็จแล้ว | `~/Downloads/POSN.Computer/ข้อสอบเทียม/` |

**เครื่องใหม่หรือของหาย**: `bash ~/.claude/skills/posn/install.sh` (ลงให้ครบในครั้งเดียว)
เช็คอย่างเดียวไม่ติดตั้ง: `install.sh --check` รายละเอียดดู [INSTALL.md](docs/INSTALL.md)

## Quick Start

```bash
export PATH="$(ls -d $HOME/.TinyTeX/bin/*/):$PATH"
cd <scratchpad>/exam

python3 verify.py                      # 1. ตรวจคำตอบทุกข้อก่อน
xelatex -interaction=nonstopmode examN.tex   # 2. คอมไพล์ 2 รอบ (lastpage ต้องใช้)
xelatex -interaction=nonstopmode examN.tex
python3 scripts/check_answer_key.py examN.tex   # 3. ตรวจตารางเฉลย
pdftoppm -r 100 -png examN.pdf pg       # 4. ดูหน้าจริงด้วยตา (Read tool)

cp examN.pdf "$HOME/Downloads/POSN.Computer/ข้อสอบเทียม/ข้อสอบเทียม_สอวนคอมพิวเตอร์_ชุดที่N.pdf"
~/.local/bin/rclone copy "<file>" gdrive:POSN.Computer   # 5. อัปขึ้น Drive
```

---

## 1. Preamble มาตรฐาน (คัดลอกไปใช้ได้เลย)

```latex
\documentclass[14pt]{extarticle}
\usepackage[a4paper,top=2.2cm,bottom=2.0cm,left=2.2cm,right=2.0cm,headsep=8pt]{geometry}
\usepackage{fontspec}
\usepackage{amsmath,amssymb}
\usepackage{enumitem}
\usepackage{fancyhdr}
\usepackage{array}
\usepackage{tikz}
\usepackage{lastpage}
\usepackage{needspace}
\usetikzlibrary{calc}

\setmainfont{THSarabunNew}[
  Extension = .ttf,
  UprightFont = *,
  BoldFont = * Bold,
  ItalicFont = * Italic,
  BoldItalicFont = * BoldItalic,
  Scale = 1.112]
\XeTeXlinebreaklocale "th"
\XeTeXlinebreakskip = 0pt plus 1pt

\setlength{\parindent}{0pt}
\linespread{1.06}          % เฉลยใช้ 1.04

\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0pt}
\fancyhead[R]{\small หน้า~\thepage/\pageref{LastPage}}
```

### ทำไมต้องเป็นแบบนี้ (อย่าเปลี่ยนโดยไม่มีเหตุผล)

- **อ้างชื่อไฟล์ + `Extension` ไม่ใช่ชื่อ family** — `\setmainfont{TH Sarabun New}` จะพังด้วย
  `The font "TH Sarabun New BoldItalic" cannot be found` เพราะ fontconfig ตั้งชื่อหน้า
  BoldItalic ไม่ตรงกับที่ fontspec เดา ต้องอ้างไฟล์ตรง ๆ
  ไม่ต้องใส่ `Path=` เพราะ `install.sh` ก๊อปฟอนต์เข้า `TEXMFHOME/fonts/truetype/public/thsarabunnew/`
  แล้ว kpathsea หาเจอเอง (portable ข้ามเครื่อง) ถ้าคอมไพล์แล้วหาฟอนต์ไม่เจอ ให้รัน
  `bash ~/.claude/skills/posn/install.sh --check` ก่อน อย่าเติม path เต็มกลับเข้าไป
- **`\XeTeXlinebreaklocale "th"` จำเป็นเสมอ** — ไม่มีบรรทัดนี้ ภาษาไทยจะไม่ตัดบรรทัดเลย
  ข้อความจะทะลุขอบขวาออกนอกหน้า
- **`extarticle` 14pt ไม่ใช่ `article` 12pt** — ขนาด class คุมขนาดสูตรคณิตศาสตร์ (Computer
  Modern) ถ้าลดเป็น 12pt สูตรจะเล็กกว่าตัวหนังสือไทยจนดูไม่สมดุล **ปรับขนาดตัวหนังสือไทย
  ที่ `Scale` เท่านั้น อย่าแตะ class size**
- **`Scale = 1.112`** — ค่านี้วัดมาให้ตรงกับข้อสอบจริงปี 68 แล้ว (ดูหัวข้อ 4)
- **`lastpage` ต้องคอมไพล์ 2 รอบ** ไม่งั้นเลขหน้ารวมจะเป็น `??`

---

## 2. Macro ตัวเลือก ก/ข/ค/ง

```latex
\newcommand{\chfour}[4]{%      % ตัวเลือกสั้น เรียง 4 คอลัมน์
  \par\vspace{4pt}\noindent\hspace*{1.4em}%
  \begin{tabular}{@{}p{0.20\textwidth}p{0.20\textwidth}p{0.20\textwidth}p{0.20\textwidth}@{}}
    ก.~~#1 & ข.~~#2 & ค.~~#3 & ง.~~#4
  \end{tabular}\par\vspace{3pt}}
\newcommand{\chtwo}[4]{%       % ตัวเลือกยาวปานกลาง 2x2
  \par\vspace{4pt}\noindent\hspace*{1.4em}%
  \begin{tabular}{@{}p{0.42\textwidth}p{0.42\textwidth}@{}}
    ก.~~#1 & ข.~~#2 \\[4pt]
    ค.~~#3 & ง.~~#4
  \end{tabular}\par\vspace{3pt}}
\newcommand{\chstack}[4]{%     % ตัวเลือกยาวมาก เรียงลงมา 4 บรรทัด
  \par\vspace{3pt}\noindent\hspace*{1.4em}ก.~~#1\par\vspace{1pt}%
  \noindent\hspace*{1.4em}ข.~~#2\par\vspace{1pt}%
  \noindent\hspace*{1.4em}ค.~~#3\par\vspace{1pt}%
  \noindent\hspace*{1.4em}ง.~~#4\par\vspace{2pt}}
\newcommand{\note}[1]{\par\vspace{1pt}\noindent\hspace*{1.4em}\textbf{หมายเหตุ}~~#1\par}

\newlist{qlist}{enumerate}{1}
\setlist[qlist]{label=\arabic*., leftmargin=1.5em, itemsep=7pt, topsep=6pt}
```

**ต้องใช้ `tabular` + `p{}` ไม่ใช่ `\makebox`** — `\makebox` ไม่ตัดบรรทัด ตัวเลือกยาว
จะเขียนทับตัวเลือกถัดไป (เคยพลาดมาแล้วในข้อ 13 กับ 22 ชุดที่ 1)

**เลือก macro ให้ถูก**: ถ้าตัวเลือกยาวเกิน ~20 ตัวอักษรไทยให้ใช้ `\chtwo`
ถ้ายาวเกินครึ่งบรรทัดให้ใช้ `\chstack` ดูผลจริงจากภาพ PNG เสมอ

**กันโจทย์ขาดหน้า**: ใส่ `\Needspace{5\baselineskip}` ก่อน `\item` ทุกข้อ (เฉลยใช้ 4)
ไม่งั้นจะเจอโจทย์อยู่ท้ายหน้า แต่ตัวเลือกไปอยู่หน้าถัดไป

---

## 3. โครงสร้างข้อสอบ

### ปก (หน้า 1)
ชื่อเรื่อง → ตารางกรอกชื่อ-สกุล/เลขประจำตัวสอบ/สถานที่สอบ/ห้องสอบ + ข้อสอบวิชา/รหัสชุดวิชา/
วันสอบ/เวลา → เส้นคั่น → คำชี้แจง 6 ข้อ (ข้อสุดท้ายคือ disclaimer ว่าเป็นข้อสอบเทียม)

คัดลอกโครงจากไฟล์ชุดที่ 1/2 ได้เลย เปลี่ยนแค่เลขชุดกับรหัสชุดวิชา (`000000N`)

### สัดส่วนเนื้อหา 50 ข้อ (อิง `เนื้อหาที่ใช้สอบ.pdf`)

| ข้อ | หัวข้อ |
|---|---|
| 1-12 | จำนวนจริง ทฤษฎีจำนวน mod โครงสร้างตัวหาร แฟกทอเรียล การหารลงตัวที่ต้องใช้การให้เหตุผล |
| 13-22 | ฟังก์ชัน อินเวอร์ส สมการ/อสมการค่าสัมบูรณ์ พาราโบลา พหุนาม กำไรสูงสุด |
| 23-27 | เซต ผลคูณคาร์ทีเซียน เพาเวอร์เซต ตรรกศาสตร์ ตารางค่าความจริง |
| 28-36 | สามเหลี่ยมคล้าย วงกลม เรขาคณิตวิเคราะห์ รูปหลายเหลี่ยม พีทาโกรัส โจทย์พื้นที่ |
| 37-45 | การนับ เรียงสับเปลี่ยนวงกลม จัดหมู่ รังนกพิราบ เพิ่มเข้า-ตัดออก เส้นทางบนตาราง |
| 46-50 | ลำดับเลขคณิต/เรขาคณิต ความสัมพันธ์เวียนเกิดที่ต้องตั้งเอง โจทย์งาน โจทย์อัตราเร็ว |

หัวข้อในตารางเป็นเพียง *ขอบเขตเนื้อหา* ไม่ใช่แบบโจทย์ — ทุกข้อยังต้องผ่านบททดสอบ
"เปลี่ยนตัวเลขแล้วต้องออกแรงเท่าเดิมไหม" ในหัวข้อถัดไป

### แนวโจทย์ที่ต้องการ (สำคัญที่สุดของหัวข้อ 3)

ข้อสอบพาร์ทคณิตศาสตร์ของ สอวน. คอมพิวเตอร์ ค่าย 1 คัดคนที่ **คิดเป็น** ไม่ใช่คนที่ **ถึก**
โจทย์ทุกข้อต้องมี **ไอเดียชี้ขาดอย่างน้อยหนึ่งอย่าง** ที่ถ้ามองออกแล้วจบเร็ว
ถ้ามองไม่ออกก็ทำไม่ได้เลย — ไม่ใช่ข้อที่ทุกคนทำได้เหมือนกันหมดแต่แพ้กันที่ความอึด

**บททดสอบว่าเป็นข้อถึกหรือไม่** ถามตัวเองว่า *"ถ้าเปลี่ยนตัวเลขในโจทย์ ต้องออกแรงเท่าเดิมไหม"*

- เปลี่ยนตัวเลขแล้วยังใช้ไอเดียเดิมตอบได้เร็ว → **ข้อดี** เก็บไว้
- เปลี่ยนตัวเลขแล้วต้องไล่ใหม่หมดด้วยแรงงานเท่าเดิม → **ข้อถึก** ทิ้งหรือเขียนใหม่

| ต้องการ (ทักษะกระบวนการคิด) | หลีกเลี่ยง (แรงงานล้วน) |
|---|---|
| ตัวไม่แปรผัน (invariant) / ความเป็นคู่-คี่ | ไล่คาบของลำดับ mod แล้วเอา $n \bmod$ คาบ |
| รังนกพิราบที่ต้องสร้างกรณีแย่ที่สุดเอง | หลักหน่วยของ $a^n$ ที่วนคาบ 4 (ซ้ำซากมาก) |
| ค่าสุดขีด — ต้องอธิบายว่าทำไมดีกว่านี้ไม่ได้ | ไล่ตัวหารทั้งหมดแล้วนับ |
| นับด้วยการจับคู่ / นับส่วนเติมเต็ม | แทนสูตรผลบวกลำดับตรง ๆ จบ |
| ต้องยกตัวอย่างยืนยันว่าทำได้จริง | อสมการค่าสัมบูรณ์หลายชั้นที่เป็นขั้นตอนกล |
| ตรรกศาสตร์/อนุมาน คนโกหก-คนพูดจริง | คูณ-หารเลขหลายหลักด้วยมือ |
| ตั้งความสัมพันธ์เวียนเกิดเอง (ตัวความสัมพันธ์คือไอเดีย) | คำนวณพจน์ที่ 2570 ของความสัมพันธ์เวียนเกิด |
| แบ่งกรณีแล้วตัดกรณีที่เป็นไปไม่ได้ทิ้ง | ไล่ทุกกรณีเพราะไม่มีวิธีตัด |
| กระบวนการที่ต้องพิสูจน์ว่าหยุด/ไม่หยุด | แทนค่าลงสูตรที่โจทย์ให้มาแล้ว |

**โควตาต่อชุด 50 ข้อ**

- ข้อที่มีไอเดียชี้ขาด: **อย่างน้อย 25 ข้อ**
- ข้อกลไกล้วน (แทนสูตรจบ): ไม่เกิน 15 ข้อ — ยังต้องมีเพื่อคุมระดับความยากไม่ให้โหดเกิน
- ข้อที่ต้องไล่ค่าซ้ำ ๆ หลายรอบ (คาบ, ตารางยาว): **ไม่เกิน 2 ข้อทั้งชุด**

**เลขใหญ่ใช้ได้ ถ้ามันบังคับให้ต้องมีไอเดีย** ข้อสอบจริงชอบใช้เลขปี (2568/2569) เป็นเลขชี้กำลัง
เราใช้ได้เหมือนกัน แต่ต้องแยกให้ออก

- ดี: $7^{2569} \bmod 9$ — สังเกตครั้งเดียวว่า $7^3 \equiv 1$ แล้วจบ ไม่ต้องไล่อะไรเลย
- ไม่ดี: $a_{2570}$ ของความสัมพันธ์เวียนเกิด — ต้องไล่ 24 พจน์ก่อนถึงจะเริ่มตอบได้

### ระดับความยาก
เทียบจากข้อสอบจริงปี 64-68 โดยตรง — ส่วนใหญ่แก้ได้ใน 2-4 ขั้น ไม่ต้องใช้เครื่องคิดเลข
เลขลงตัวสวย มีข้อยากจริง ๆ 5-8 ข้อ

"ยาก" ในที่นี้แปลว่า **มองไอเดียออกยาก** ไม่ใช่ **คำนวณเยอะ** ถ้าข้อไหนยากเพราะเลขเยอะ
ให้ลดเลขลงแล้วเพิ่มชั้นการคิดแทน

### การกระจายคำตอบ
ก/ข/ค/ง ต้องใกล้เคียงกัน (~12-13 ข้อต่อตัวเลือก) **ห้ามเรียงตัวเลือกจากน้อยไปมากทุกข้อ**
เพราะจะทำให้เดาตำแหน่งคำตอบได้ ข้อสอบจริงก็ไม่เรียง (ปี 68 ข้อ 1: 749, 901, 911, 897)
นับแล้วปรับก่อนคอมไพล์

---

## 4. ขนาดฟอนต์ — วิธีวัดให้ตรงข้อสอบจริง

`Scale = 1.112` มาจากการวัด ไม่ใช่การกะ ถ้าต้องปรับใหม่ (เช่นเทียบปีอื่น) ใช้
`scripts/measure_font_size.py`:

```bash
python3 scripts/measure_font_size.py "ข้อสอบสอวนคอมปี 68.pdf" examN.pdf
# → median ratio mine/real = 0.9994   (1.00 = ตรงเป๊ะ)
```

**หลักการ**: `pdftotext -bbox` คืนกล่องฟอนต์ (font ascent/descent) ไม่ใช่กล่องหมึก
ทุกคำในบรรทัดเดียวกันจึงสูงเท่ากัน = เทียบขนาดฟอนต์ได้ตรง ๆ สคริปต์จะจับคู่
**คำภาษาไทยคำเดียวกัน** ที่ปรากฏในทั้งสองไฟล์ (เช่น "และ" "ที่สอดคล้องกับ") แล้วหา median
ของอัตราส่วนความสูง

เทียบได้เพราะเป็นฟอนต์ตระกูลเดียวกัน ถ้าคนละฟอนต์ตัวเลขนี้ใช้ไม่ได้

**ตัวเลขอ้างอิง**: ปี 68 คำไทยสูง 17.56 pt / ปี 67 สูง 16.50 pt (เล็กกว่า 6%)
ค่ามาตรฐานที่ใช้คือ **จับคู่ปี 68** เพราะเป็นฉบับล่าสุด → `Scale = 1.112` ให้ 17.55 pt

**สิ่งที่รู้แต่ตั้งใจไม่ทำตาม**: ข้อสอบจริงเป็นกระดาษ Letter (612×792 pt) เพราะทำจาก Word
เราใช้ A4 เพราะปรินต์ในไทยสะดวกกว่า ถ้าผู้ใช้ขอ "เหมือนจริงทุกอย่าง" จริง ๆ
ค่อยเปลี่ยนเป็น `letterpaper`

---

## 5. ขั้นตอนตรวจคำตอบ (ทำก่อนเขียน .tex เสมอ)

เขียน `verify.py` ที่คำนวณคำตอบทุกข้อด้วยวิธีตรงไปตรงมาที่สุด (brute force ดีกว่าสูตรปิด
เพราะกันพลาดจากการคิดสูตรผิด) เช่น

```python
R[1] = sum(n for n in range(2,500) if n%6==1 and n%8==1 and n%9==1)   # ไม่ใช้ lcm
R[37] = sum(1 for n in range(1000,10000) if n%2==1 and len(set(str(n)))==4)
R[42] = q42()   # นับ partition จริง ไม่ใช้ C(8,4)/2 - C(6,2)
```

สำหรับอสมการให้ sample ค่า x ถี่ ๆ แล้วดูช่วงที่เป็นจริง อย่าแก้ด้วยมืออย่างเดียว
สำหรับลำดับ mod ให้หา period จริงแล้ว print ออกมาเทียบกับที่จะเขียนในเฉลย

ผลลัพธ์ที่เป็นเศษส่วนให้ใช้ `fractions.Fraction` ไม่ใช้ float

---

## 6. ไฟล์เฉลย — วิธีที่ง่ายที่สุด แสดงให้ครบทุกขั้น

โครงสร้าง: หัวเรื่อง → **ตารางเฉลย 50 ข้อ** (ตาราง 6 แถวคู่ ข้อ/ตอบ) → เส้นคั่น →
**แนวคิดและวิธีทำ** ทีละข้อ

### เป้าหมายของเฉลย

ผู้ใช้อ่านเฉลยเพื่อ **เข้าใจ** ไม่ใช่เพื่อเช็คคำตอบ (ตารางหน้าแรกเช็คให้แล้ว)
เกณฑ์ตัดสิน: **นักเรียนที่ทำข้อนั้นไม่ได้เลย ต้องอ่านเฉลยแล้วทำข้อคล้าย ๆ กันได้เอง
โดยใช้วิธีนั้นในห้องสอบจริงได้ด้วย**

มีสองด้านที่ต้องได้พร้อมกัน อย่าแลกอันหนึ่งกับอีกอัน:

| ด้าน | หมายความว่า | ที่พังบ่อย |
|---|---|---|
| **เรียบง่าย** | เลือกวิธีที่สั้นที่สุดที่ใช้ได้จริงในห้องสอบ | ใช้เครื่องมือเกินระดับเพราะดู "ครบเครื่อง" |
| **ครบขั้น** | วิธีที่เลือกแล้ว ต้องไม่ข้ามพีชคณิตแม้แต่ขั้นเดียว | ย่อเพราะคิดว่า "ข้อนี้ง่าย" |

**ห้ามเขียนเฉลยแบบย่อ** เช่น "แทนค่าได้ 17{,}010" หรือ "จาก AM-GM ได้ 12"
— กระโดดขั้นแบบนี้คือสิ่งที่ต้องแก้

### เลือกวิธีทำอย่างไร (ทำก่อนเขียนเฉลยทุกข้อ)

ก่อนลงมือเขียน ให้คิดวิธีที่เป็นไปได้ 2-3 วิธีก่อน แล้วเลือกด้วยเกณฑ์นี้ **ตามลำดับ**:

1. **ทำได้จริงในห้องสอบ** — นักเรียน ม.4-5 ไม่มีเครื่องคิดเลข มีเวลาต่อข้อประมาณ 3 นาที
   วิธีที่สวยทางคณิตศาสตร์แต่ทำในห้องสอบไม่ทัน = ตกรอบทันที
2. **เห็นภาพ** — รูป ตาราง หรือการไล่ค่าจริง ชนะสูตรนามธรรมเสมอเมื่อจำนวนกรณีน้อย
3. **ใช้ซ้ำกับข้อคล้าย ๆ กันได้** — เลือกวิธีมาตรฐานที่ใช้ได้ทั้งตระกูลโจทย์
   ไม่ใช่ทริกเฉพาะข้อที่ใช้ได้ครั้งเดียว
4. **สั้นกว่าชนะ** — ถ้าสองวิธีดีพอกันหมด เอาวิธีที่ขั้นน้อยกว่าเป็นวิธีหลัก
   อีกวิธีใส่ท้ายเป็น "ตรวจอีกทาง" บรรทัดเดียวพอ

**ห้ามทำ**

- ใช้เครื่องมือเกินระดับ เช่น อนุพันธ์หาค่าสูงสุดของพาราโบลา (ใช้จุดยอด $-\frac{b}{2a}$),
  ผลคูณเชิงสเกลาร์เมื่อผลคูณความชัน $=-1$ ก็จบ, ทฤษฎีบทนอกขอบเขต `เนื้อหาที่ใช้สอบ.pdf`
- ใช้สูตรปิดซับซ้อนเมื่อจำนวนกรณีน้อยกว่า 10 — ไล่ตารางให้ดูเลย เห็นชัดกว่าและตรวจง่ายกว่า
- ยืดขั้นตอนให้ครบโควตา **จำนวนขั้นมีเท่าที่วิธีนั้นต้องใช้จริง** ถ้าจบได้ใน 3 ขั้น ห้ามยืดเป็น 5
- อธิบายเป็นย่อหน้ายาว ทั้งที่รูปหรือตาราง 4 บรรทัดจบ

**ตัวอย่างจริงที่เลือกถูกแล้ว** (จากชุดที่ 2 ใช้เป็นแบบอ้างอิงได้)

| ข้อ | เลือกใช้ | แทนที่จะใช้ |
|---|---|---|
| กำไรสูงสุด | จุดยอดพาราโบลา $q=-\frac{b}{2a}$ | อนุพันธ์ |
| มุมฉากที่จุด $B$ | ผลคูณความชัน $=-1$ (เห็นภาพ) | ผลคูณเชิงสเกลาร์ (ใส่เป็นตัวตรวจท้ายข้อ) |
| ที่ดินพื้นที่ 60-95 | ไล่ตาราง $w=3,\ldots,8$ | แก้อสมการกำลังสองสองชั้น |
| ซื้อของเล่น 5 ชิ้น | ตาราง 6 กรณีพร้อมราคารวม | stars and bars + ตัดกรณี |
| เส้นสูงจากมุมฉาก | คิดพื้นที่สองแบบแล้วจับเท่ากัน | สามเหลี่ยมคล้ายสามรูป |
| เศษเหลือ mod | จัดกลุ่มตามเศษ + ตารางกลุ่ม | ทฤษฎีบทเชิงสัญลักษณ์ |

### ใส่ "ตัวตรวจเร็ว" ให้ทุกข้อที่ทำได้

สิ่งที่ใช้ได้จริงที่สุดในห้องสอบคือวิธีตัดตัวเลือกทิ้งโดยไม่ต้องคำนวณซ้ำ ให้หาตัวตรวจแบบนี้
แล้วใส่ไว้ในส่วน "ทำไมตัวเลือกอื่นผิด" เสมอเมื่อมี เช่น

- ผลบวกของ 17 พจน์ต้องหารด้วย 17 ลงตัว → ตัดตัวลวงได้ 3 ตัวรวด
- คู่ทั้งหมด $\binom{50}{2}=1{,}225$ กระจายเท่ากัน 5 เศษ → คำตอบต้องเป็น $1{,}225/5=245$
- เส้นตั้งฉากจากมุมฉากต้องสั้นกว่าเส้นมัธยฐาน $=5$ → ตัด $\frac{28}{5}=5.6$ ทิ้งทันที
- พื้นที่ $=84\times\frac{k}{21}=4k$ → คำตอบต้องเป็นพหุคูณของ 4

### โครงบังคับของแต่ละข้อ

```latex
\ansline{ค.\ $17{,}010$}{}

\textbf{แนวคิด}~~ ...ประโยคเดียวว่าโจทย์ข้อนี้ทดสอบเรื่องอะไร และทำไมต้องใช้วิธีนี้...

\textbf{วิธีทำ}
\stepa{1}{...สิ่งที่โจทย์ให้ / ตั้งตัวแปร...}
\stepa{2}{...ขั้นคิด พร้อม \[ สมการเต็ม \]...}
\stepa{3}{...}
\textbf{ตอบ} ...

\textbf{ทำไมตัวเลือกอื่นผิด}~~ ก. ...  ข. ...  ง. ...

\textbf{จุดพลาดที่พบบ่อย}~~ ...(ใส่เมื่อข้อนั้นมีกับดักจริง)...
```

5 ส่วนนี้ **บังคับทุกข้อ** ยกเว้น "จุดพลาด" ที่ใส่เฉพาะข้อที่มีกับดัก

Macro ที่ต้องเพิ่มใน preamble ของไฟล์เฉลย:
```latex
\newcommand{\stepa}[2]{\par\vspace{3pt}\noindent\hspace*{1.2em}%
  \textbf{ขั้นที่ #1)}~~\parbox[t]{0.82\textwidth}{#2}\par\vspace{2pt}}
```
ใช้ `\parbox` ไม่ใช่ `itemize` — `\item` ซ้อนทำให้ `check_answer_key.py` นับข้อเพี้ยน
(ดู TROUBLESHOOTING.md)

### กฎรายละเอียด

1. **ทุกขั้นต้องมีเหตุผลกำกับ ไม่ใช่แค่ผลลัพธ์** เขียน "ยกกำลังสองทั้งสองข้างเพื่อกำจัดราก
   ทำได้เพราะสองข้างเป็นบวก" ไม่ใช่แค่เขียนสมการที่ยกกำลังสองแล้ว
2. **ห้ามข้ามพีชคณิต** ถ้าจาก $3x+7=22$ ไป $x=5$ ให้แสดง $3x=15$ ตรงกลางด้วย
3. **สูตรที่ใช้ต้องเขียนรูปทั่วไปก่อนแทนค่า** เช่น
   \[ \binom{n}{r}=\frac{n!}{r!\,(n-r)!} \quad\Rightarrow\quad \binom{8}{3}=\frac{8!}{3!\,5!}=56 \]
   ไม่ใช่โผล่ 56 มาเฉย ๆ ชื่อทฤษฎีบทต้องเรียกให้ถูก (ทฤษฎีบทเศษเหลือ, อสมการสามเหลี่ยม,
   หลักรังนกพิราบ, ทฤษฎีบทของวีเอต)
4. **โจทย์เรขาคณิตต้องมีรูป** วาดด้วย TikZ ในเฉลยด้วยเสมอ แม้โจทย์จะมีรูปแล้ว
   เพราะเฉลยต้องเพิ่มเส้นช่วย/ชื่อจุด/มุมที่ใช้จริง พิกัดต้องคำนวณ ห้ามกะ
5. **ข้อนับ (counting)** ต้องบอกว่านับแบบไหน (เรียงลำดับ/ไม่เรียง) แยกกรณีให้ครบ
   และบอกว่าทำไมกรณีไม่ทับกัน ถ้าใช้เพิ่มเข้า-ตัดออก ให้เขียนทุกพจน์
6. **ตรรกศาสตร์** พิมพ์ตารางค่าความจริงเป็น `tabular` เต็มตาราง 4 แถว ไม่ใช่สรุปเป็นประโยค
7. **ลำดับ/mod** (ควรมีไม่เกิน 2 ข้อต่อชุด ดูหัวข้อ 3) พิมพ์พจน์จริงหลายพจน์พร้อม
   `\underbrace{}_{\text{ซ้ำเดิม}}` ชี้จุดวนซ้ำ แล้วบอกคาบ (period) ชัด ๆ ว่าเท่าไร
   และ $n \bmod \text{period}$ ตกที่พจน์ไหน **ต้องตรวจพจน์ที่พิมพ์ทีละตัวกับสคริปต์** ก่อนส่ง
8. **ตัวเลือกลวงต้องอธิบาย** ตัวลวงถูกออกแบบจากความผิดที่คนทำจริง — ระบุให้ตรง
   เช่น "ข. คือคำตอบของคนที่ลืมกรณี $x<0$" ไม่ใช่ "ข. ผิดเพราะคำนวณผิด"
9. **ผลลัพธ์เศษส่วนคงรูปเศษส่วน** ห้ามแปลงเป็นทศนิยม ยกเว้นโจทย์ถามทศนิยม
10. **ความยาวเป็นผลลัพธ์ ไม่ใช่เป้าหมาย** เฉลยที่ครบขั้นจริงมักออกมา 20-45 หน้าต่อชุด
    แต่ห้ามเล็งจำนวนหน้า — **ห้ามตัดขั้นตอนเพื่อให้สั้น และห้ามใช้วิธีที่อ้อมกว่าเพื่อให้ยาว**
    ยาวเพราะแสดงครบ = ดี ยาวเพราะเลือกวิธีซับซ้อน = ต้องเขียนใหม่ด้วยวิธีที่ง่ายกว่า

### ตัวอย่างเทียบ

ไม่เอา:
> \ansline{ค.\ $56$}{} จาก $\binom{8}{3}=56$

เอาแบบนี้:
> \ansline{ค.\ $56$}{}
>
> \textbf{แนวคิด}~~ เลือกคน 3 คนจาก 8 คน โดยไม่สนลำดับที่เลือก จึงเป็นการจัดหมู่ (combination)
> ไม่ใช่การเรียงสับเปลี่ยน
>
> \textbf{วิธีทำ}
> \stepa{1}{สังเกตว่า "คณะกรรมการ" ไม่มีตำแหน่ง สลับชื่อกันแล้วได้คณะเดิม จึงไม่นับลำดับ}
> \stepa{2}{สูตรจัดหมู่ $\displaystyle\binom{n}{r}=\frac{n!}{r!\,(n-r)!}$ ในที่นี้ $n=8$, $r=3$}
> \stepa{3}{แทนค่า $\displaystyle\binom{8}{3}=\frac{8!}{3!\,5!}=\frac{8\times7\times6}{3\times2\times1}=\frac{336}{6}=56$}
> \textbf{ตอบ} $56$ วิธี
>
> \textbf{ทำไมตัวเลือกอื่นผิด}~~ ก. $24$ คือ $8\times3$ คูณมั่ว  ข. $\ 40$ คือ $\binom{8}{3}$ ที่หารผิด
> ง. $336$ คือ $P(8,3)$ ของคนที่นับลำดับด้วย
>
> \textbf{จุดพลาดที่พบบ่อย}~~ ใช้ $P(8,3)=336$ เพราะเผลอนับลำดับ ให้ถามตัวเองเสมอว่า
> "สลับตำแหน่งกันแล้วเป็นคำตอบเดิมไหม" ถ้าเดิม = จัดหมู่

### ตรวจตารางเฉลยอัตโนมัติ

```bash
python3 scripts/check_answer_key.py examN.tex answerN.tex
```
สคริปต์จะ parse `\chfour/\chtwo/\chstack` ออกมา แล้วเทียบว่าตัวเลือกในตำแหน่งที่ตาราง
เฉลยชี้ ตรงกับค่าที่คาดไว้จริงหรือไม่ ข้อที่เป็นข้อความล้วนสคริปต์ตรวจไม่ได้ ต้องไล่ดูเอง

---

## 7. การตั้งชื่อไฟล์และที่เก็บ

```
~/Downloads/POSN.Computer/
├── ข้อสอบเทียม/                                        ← ไฟล์ที่เราสร้าง อยู่ที่นี่
│   ├── ข้อสอบเทียม_สอวนคอมพิวเตอร์_ชุดที่N.pdf
│   └── เฉลย_ข้อสอบเทียม_สอวนคอมพิวเตอร์_ชุดที่N.pdf
├── ข้อสอบสอวนคอมปี 64-68.pdf                          ← ข้อสอบจริง อย่าแตะ
└── เนื้อหาที่ใช้สอบ.pdf
```

ไม่มีช่องว่างหลัง "ชุดที่" (ชุดที่1 ไม่ใช่ ชุดที่ 1) ตามที่ผู้ใช้กำหนด

ไฟล์ `.tex` เก็บใน scratchpad ของ session ไม่ต้องเอาไปไว้ในโฟลเดอร์ผู้ใช้ นอกจากจะสั่ง

---

## 8. อัปขึ้น Google Drive

```bash
~/.local/bin/rclone copy "<ไฟล์>" gdrive:POSN.Computer
~/.local/bin/rclone ls gdrive:POSN.Computer          # ยืนยันขนาดตรงกับไฟล์ต้นทาง
```

**อย่าใช้ `rclone sync`** — `sync` ลบไฟล์ปลายทางที่ไม่มีในต้นทาง ใช้ `copy` เท่านั้น

**อย่ากรอง output ของ rclone ทิ้งทั้งหมด** — เคย `grep -v NOTICE` แล้วมองไม่เห็นว่า rclone
รายงานอะไรบ้าง ทำให้ตามเรื่องไฟล์หายยากขึ้น ถ้าจะกรอง ให้เก็บ log ไว้ดูด้วย

ยืนยันซ้ำด้วย MCP tool เพื่อเช็ค mimeType:
```
mcp__claude_ai_Google_Drive__search_files
  query: parentId = '<FOLDER_ID>' and title contains 'ชุดที่N'
```
ต้องได้ `application/pdf` ไม่ใช่ `application/vnd.google-apps.document`

**เส้นทางที่ใช้ไม่ได้ (ลองมาแล้ว อย่าเสียเวลาซ้ำ)** — ดู [DRIVE.md](docs/DRIVE.md)

---

## 9. Checklist ก่อนส่งมอบ

- [ ] `verify.py` รันผ่าน ทุกข้อมีคำตอบที่คำนวณได้
- [ ] `check_answer_key.py` ผ่าน 100%
- [ ] ข้อความล้วน (ตรรกศาสตร์/ช่วง/สูตร) ไล่ตรวจด้วยตาแล้ว
- [ ] การกระจาย ก/ข/ค/ง สมดุล
- [ ] คอมไพล์ 2 รอบ ไม่มี `^!` ใน log
- [ ] ดูภาพ PNG ทุกหน้า ไม่มีตัวเลือกทับกัน ไม่มีโจทย์ขาดหน้า ไม่มีข้อความล้นขอบ
- [ ] `measure_font_size.py` ได้ ratio ≈ 1.00
- [ ] ไม่มีโจทย์ซ้ำกับชุดก่อนหน้า
- [ ] **ทุกข้อผ่านบททดสอบ "เปลี่ยนตัวเลขแล้วต้องออกแรงเท่าเดิมไหม"** — ข้อถึกต้องเขียนใหม่
- [ ] ข้อที่มีไอเดียชี้ขาด ≥ 25 ข้อ / ข้อไล่ค่าซ้ำ ๆ (คาบ ตารางยาว) ≤ 2 ข้อ
- [ ] **เฉลยครบ 5 ส่วนทุกข้อ** — สุ่มเปิด 5 ข้อ อ่านแล้วตามได้โดยไม่ต้องเติมขั้นเอง
- [ ] **สุ่มเปิด 5 ข้อแล้วถามว่า "มีวิธีที่ง่ายกว่านี้ไหม"** ถ้ามี ต้องเขียนใหม่ด้วยวิธีนั้น
- [ ] ไม่มีข้อไหนใช้เครื่องมือเกินขอบเขต `เนื้อหาที่ใช้สอบ.pdf`
- [ ] ข้อที่มีกรณีน้อยกว่า 10 กรณี ใช้ตารางไล่กรณี ไม่ใช่สูตรปิด
- [ ] ทุกข้อที่หาตัวตรวจเร็วได้ มีตัวตรวจอยู่ในส่วน "ทำไมตัวเลือกอื่นผิด"
- [ ] เฉลยไม่มีข้อไหนสั้นกว่า 4 บรรทัด (แต่ไม่ต้องเล็งจำนวนหน้ารวม)
- [ ] ข้อเรขาคณิตทุกข้อในเฉลยมีรูป TikZ พร้อมเส้นช่วย
- [ ] คัดลอกลง `ข้อสอบเทียม/` แล้ว และอัปขึ้น Drive แล้ว ยืนยัน mimeType แล้ว

---

## เอกสารเพิ่มเติม

- [INSTALL.md](docs/INSTALL.md) — ย้ายไปเครื่องใหม่ (แพ็ก → ติดตั้ง → ตรวจ)
- `install.sh` — ติดตั้ง/ตรวจทุกอย่างในคำสั่งเดียว
- [SETUP.md](docs/SETUP.md) — ขั้นตอนติดตั้งแบบทำมือ ถ้า `install.sh` พัง
- [DRIVE.md](docs/DRIVE.md) — รายละเอียดการอัป Drive และวิธีที่ใช้ไม่ได้
- [TROUBLESHOOTING.md](docs/TROUBLESHOOTING.md) — error ที่เคยเจอจริงและวิธีแก้
- `scripts/check_answer_key.py` — ตรวจตารางเฉลยกับตัวเลือก
- `scripts/measure_font_size.py` — วัดขนาดฟอนต์เทียบข้อสอบจริง
