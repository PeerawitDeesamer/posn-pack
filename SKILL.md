---
name: "POSN Mock Exam Builder"
description: "สร้างข้อสอบเทียม สอวน. วิชาคอมพิวเตอร์ (ค่าย 1) พร้อมเฉลย เป็น PDF ด้วย XeLaTeX ฟอนต์ TH Sarabun New ขนาดเท่าข้อสอบจริง แล้วอัปขึ้น Google Drive. เลือกได้ว่าเอาพาร์ทคณิตศาสตร์หรือพาร์ทคอมพิวเตอร์ (วิทยาการคำนวณ) แบบปรนัยหรืออัตนัยเติมคำตอบ ระดับความยาก และหัวข้อที่ต้องการเน้น. Use when creating/editing POSN mock exams (ข้อสอบเทียม, ข้อสอบจำลอง สอวน.), choosing exam part/format/difficulty/topic focus (พาร์ทคอม, พาร์ทคณิต, ปรนัย, อัตนัย, ชุดยาก, เน้นเรื่อง), generating Thai exam or answer-key PDFs, matching a real Thai exam's font and layout, or uploading files to this user's Google Drive."
---

# POSN Mock Exam Builder

ข้อสอบเทียม สอวน. คอมพิวเตอร์ (คัดเลือกเข้าค่าย 1) ให้เหมือนข้อสอบจริงทุกด้าน พร้อมเฉลยแยกไฟล์
สั่งได้ว่าเอา **พาร์ทไหน** (`math`/`comp`/`both`) **รูปแบบไหน** (ปรนัย/อัตนัย/ผสม) **ยากแค่ไหน**
(4 พรีเซ็ต) และ **เน้นเรื่องอะไร**

## กฎเหล็ก (ห้ามละเมิด)

1. **คณิตศาสตร์ทุกตัวต้องพิมพ์ด้วย LaTeX** ห้ามใช้ Unicode math (²,√,≤,½) ห้ามใช้ plain text
   แทนสูตร ทุกอย่างอยู่ใน `$...$` หรือ `\[...\]`
2. **ฟอนต์ต้องเป็น TH Sarabun New** (ไม่ใช่ Sarabun ของ Google ซึ่งเป็นคนละฟอนต์)
3. **ห้ามเดาคำตอบ และห้ามเดาตัวลวง** ทั้งคำตอบถูกและตัวเลือกลวงทุกตัวต้องคำนวณจาก
   `verify.py` ก่อนพิมพ์ลงไฟล์ ตัวลวงต้องมาจากความผิดที่ระบุชื่อได้ใน `D[]` — หัวข้อ 3
4. **ต้องผ่าน `preflight.py` ครบทุกข้อก่อนส่งมอบ** ห้ามข้ามการตรวจ — หัวข้อ 6
5. ปกต้องระบุชัดว่าเป็นข้อสอบเทียมเพื่อฝึกฝน ไม่ใช่ข้อสอบจริงของมูลนิธิ สอวน.
6. **เฉลยต้องเลือกวิธีที่ง่ายและเห็นภาพที่สุดก่อน แล้วแสดงวิธีนั้นให้ครบทุกขั้น** ครบ 6 ส่วน
   และอย่างน้อย 3 ขั้นทุกข้อ ห้ามข้ามขั้นพีชคณิต ห้ามเขียนแค่ผลลัพธ์ — หัวข้อ 4
7. **โจทย์ต้องวัดการคิด ไม่ใช่ความถึก** ทุกข้อต้องมีไอเดียชี้ขาด ห้ามออกข้อที่ชนะกันด้วย
   การไล่ค่าซ้ำ ๆ (หาคาบ ไล่ตารางยาว คำนวณเลขใหญ่ด้วยมือ) เกิน 2 ข้อต่อชุด — หัวข้อ 2
8. **ต้องล็อกสเปกและสร้างพิมพ์เขียวก่อนเขียนโจทย์ข้อแรกเสมอ** แล้วพูดสเปกที่จะใช้ให้ผู้ใช้ฟัง
   ก่อน ห้ามเดาเงียบ ๆ — หัวข้อ 0
9. **โปรแกรมทุกชิ้นในพาร์ทคอมต้องรันจริง** ก่อนพิมพ์ตัวเลือก และใช้เฉพาะคำสั่งในขอบเขต — หัวข้อ 2
10. **ห้ามเขียนถ้อยคำจากข้อสอบจริงลงไฟล์ใด ๆ** (`.tex` `.py` `.json` `.md` scratchpad)
    แม้ชั่วคราว — อ่านจาก PDF ได้ แต่สิ่งที่บันทึกออกมาได้มีอย่างเดียวคือ `idea` ที่เป็นโครงคิด
    นามธรรม ไอเดียทางคณิตศาสตร์ไม่มีลิขสิทธิ์ แต่ถ้อยคำและบริบทที่แต่งขึ้นมี ([VARIANT.md](docs/VARIANT.md))
    ใช้กับ *ตัวโจทย์* เท่านั้น ประกาศขอบเขตเนื้อหาเป็นรายการหลักสูตร คัดมาอ้างได้

## Environment

| สิ่งของ | ตำแหน่ง |
|---|---|
| XeLaTeX (TinyTeX) | `~/.TinyTeX/bin/<arch>/` — ไม่มี LaTeX ระดับระบบ ไม่มี passwordless sudo |
| ฟอนต์ TH Sarabun New | `~/.local/share/fonts/thsarabunnew/` + `TEXMFHOME/fonts/truetype/public/` |
| rclone | `~/.local/bin/rclone` remote ชื่อ `gdrive` (authorized แล้ว) |
| **ข้อมูลผู้ใช้ทั้งหมด** | `~/Documents/POSN.Computer/` — ดูโครงในหัวข้อ 7 |

เครื่องใหม่หรือของหาย: `bash ~/.claude/skills/posn/install.sh` (`--check` = ตรวจอย่างเดียว)

## Quick Start

```bash
export PATH="$(ls -d $HOME/.TinyTeX/bin/*/):$PATH"
S=~/.claude/skills/posn; mkdir -p <scratchpad>/exam && cd <scratchpad>/exam

python3 $S/scripts/exam_blueprint.py --part comp --format mixed \
        --difficulty hard --focus pyloop --count 30 --set 2 --json    # 0. พิมพ์เขียว
python3 $S/scripts/next_set.py comp              # 1. เลขชุดว่างจริง (ดู Drive ด้วย)
cat ~/Documents/POSN.Computer/index.jsonl        #    กันโจทย์ซ้ำ ห้ามเปิด PDF ชุดเก่า
cp $S/assets/*.tex ./ && python3 verify.py       # 2. คำตอบ + ตัวลวง → verify_out.json
                                                 # 3. เขียน exam2.tex answer2.tex ideas.json
for f in exam2 answer2; do xelatex -interaction=nonstopmode $f; xelatex -interaction=nonstopmode $f; done
python3 $S/scripts/preflight.py comp 2           # 4. ตรวจทุกอย่าง + บอกหน้าที่ต้องดูด้วยตา
cp exam2.pdf "$HOME/Documents/POSN.Computer/ข้อสอบเทียม/พาร์ทคอมพิวเตอร์/<ชื่อ>.pdf"
~/.local/bin/rclone copy "<ไฟล์>" gdrive:POSN.Computer                # 5. ส่งมอบ
python3 $S/scripts/exam_index.py add comp 2 --ideas ideas.json
```
คอมไพล์ 2 รอบเสมอ (`lastpage` ต้องอ่าน .aux รอบก่อน ไม่งั้นเลขหน้ารวมเป็น `??`)

## 0. สเปกข้อสอบ — ทำก่อนทุกอย่าง

ชุดหนึ่งกำหนดด้วย 6 ค่า ไม่ระบุให้ใช้ค่าปริยาย **แต่ต้องบอกออกมาก่อนเริ่มเสมอ** — แก้ตอนวางแผน 1 นาที แก้ตอนเขียนเสร็จแล้ว 2 ชั่วโมง

| ค่า | ตัวเลือก | ปริยาย |
|---|---|---|
| พาร์ท | `math` / `comp` / `both` | `math` |
| รูปแบบ | `mcq` / `fill` / `mixed` | คณิต=`mcq` คอม=`mixed` |
| ความยาก | `easy` / `standard` / `hard` / `brutal` | `standard` (เท่าข้อสอบจริง) |
| เรื่องที่เน้น | รหัสหัวข้อ คั่นด้วย `,` | ไม่เน้น = กระจายตามน้ำหนัก |
| จำนวนข้อ | ตัวเลข | คณิต 50 / คอม 30 / ทั้งฉบับ 60 (โครงปี 68) |
| เลขชุด | ตัวเลข | **ต้องรัน `next_set.py <part>` เท่านั้น อย่าเดา อย่า `ls` เอาเอง** |

`--list` = รหัสหัวข้อ ระดับ พรีเซ็ต โครงข้อสอบจริงรายปี ผลที่ได้คือตาราง **ข้อที่ / หัวข้อ /
ระดับ / รูปแบบ** + โควตา + เวลา + ชื่อไฟล์ + รหัสชุดวิชา — ก๊อปไปวางเป็นคอมเมนต์หัว
`verify.py` แล้วเขียนโจทย์ไล่ตามตาราง

**`--json` เป็นขั้นบังคับ ไม่ใช่ทางเลือก** เพราะ `/posn-score` `preflight.py` และ
`exam_index.py` ใช้มันเป็นแหล่งความจริงว่าข้อไหนหัวข้ออะไรระดับไหน — ชุดที่ทำโดยไม่มี
พิมพ์เขียวคือข้อมูลที่หายถาวร

**ต้องถามกลับ ไม่ต้องเดา** (รวมทีเดียว): ขอ "เน้นเรื่อง X" ที่อยู่นอกขอบเขต
([SCOPE.md](docs/SCOPE.md)) ให้บอกตรง ๆ แล้วเสนอหัวข้อใกล้เคียง ห้ามออกข้อนอกขอบเขตเงียบ ๆ ·
แปลคำสั่งผู้ใช้เป็นสเปก ข้อความบนปก รหัสชุดวิชา: [SPEC.md](docs/SPEC.md)

## 1. ไฟล์ LaTeX

`cp ~/.claude/skills/posn/assets/{preamble-exam,preamble-answer,macros-comp}.tex ./`
แล้ว `examN.tex` บรรทัดแรก `\input{preamble-exam}` (มีพาร์ทคอมหรือตอนอัตนัยเติม
`\input{macros-comp}`) · `answerN.tex` บรรทัดแรก `\input{preamble-answer}`

**อย่าแก้ค่าในไฟล์นี้โดยไม่อ่านคอมเมนต์ในไฟล์ก่อน** ทุกค่ามีเหตุผลที่เคยพลาดมาแล้วกำกับข้าง ๆ
(`Extension` ไม่ใช่ชื่อ family / `\XeTeXlinebreaklocale` / `extarticle` 14pt / `tabular` ไม่ใช่ `\makebox`)
เลือก macro ตัวเลือกให้ถูก: ยาวเกิน ~20 ตัวอักษรไทยใช้ `\chtwo` ยาวเกินครึ่งบรรทัดใช้ `\chstack`
ใส่ `\Needspace{5\baselineskip}` ก่อน `\item` ทุกข้อ (อัตนัย 6 เฉลย 4)

## 2. โครงสร้างและแนวโจทย์

**ปก** ชื่อเรื่อง + ชื่อพาร์ท → ตารางกรอกชื่อ-สกุล/เลขที่นั่งสอบ/สถานที่/ห้องสอบ + วิชา/รหัสชุดวิชา/
วันสอบ/เวลา → เส้นคั่น → คำชี้แจง (ข้อสุดท้ายคือ disclaimer) ก๊อปโครงเดิมแล้วเปลี่ยนตามสเปก — [SPEC.md](docs/SPEC.md) · สัดส่วนหัวข้อยึดผลจาก `exam_blueprint.py`
**ส่วนขอบเขตว่าออกอะไรได้-ไม่ได้ ยึด [SCOPE.md](docs/SCOPE.md)** ซึ่งถอดจากประกาศ สอวน. 2569

**พาร์ทคอม — ขอบเขตภาษาที่ใช้ได้เท่านั้น** กำหนดค่าตัวแปร, นิพจน์คำนวณ/เปรียบเทียบ, `not and or`,
`if` เดี่ยว/ซ้อน, `while`, **ลูปซ้อนไม่เกิน 2 ชั้น**, **อาเรย์ 1 มิติ + ดัชนี `a[i]`**, `print()`
**ห้าม** `def` `for` `range()` dict/set `import` f-string ลูปซ้อนเกิน 2 ชั้น ·
`break`/`continue` `elif` `len()` `.upper()/.lower()` ใช้ได้ (ข้อสอบจริงใช้) ถ้าใส่ `\note{}` อธิบาย
และตัวดำเนินการที่ไม่คุ้น (`//` `%` `**` `!=`) ต้องมี `\note{}` ทุกครั้ง

**โปรแกรมทุกชิ้นต้องรันจริง** ใน `verify.py` โดยเก็บโค้ดเป็นสตริงเดียวแล้ว `exec()` และ print
สตริงเดียวกันนั้นออกมาวางใน `.tex` — ห้ามพิมพ์โค้ดซ้ำสองที่ ห้ามไล่ด้วยตาแล้วเชื่อ
แนวโจทย์รายหัวข้อและจุดพลาดที่พบบ่อย: [COMPUTER-PART.md](docs/COMPUTER-PART.md)

**ตอนอัตนัย** คำตอบต้องเป็น **จำนวนเต็ม 0-9999 ค่าเดียว** ห้ามเศษส่วน ทศนิยม จำนวนลบ หรือข้อความ
ข้อละ 2 คะแนนจึงห้ามต่ำกว่า L3 แนวที่เข้ากันที่สุดคือ `simulate` `greedy` `algocount`
**ออกแบบย้อนจากคำตอบ** ตั้งเลขให้ลงตัวสวยตั้งแต่แรก อย่าเขียนโจทย์แล้วมาลุ้น — [SUBJECTIVE.md](docs/SUBJECTIVE.md)

### แนวโจทย์ที่ต้องการ (สำคัญที่สุดของหัวข้อนี้)

ข้อสอบชุดนี้คัดคนที่ **คิดเป็น** ไม่ใช่คนที่ **ถึก** ทุกข้อต้องมี **ไอเดียชี้ขาดอย่างน้อยหนึ่งอย่าง**
ที่ถ้ามองออกแล้วจบเร็ว ถ้ามองไม่ออกก็ทำไม่ได้เลย ไม่ใช่ข้อที่ทุกคนทำได้แต่แพ้กันที่ความอึด
**บททดสอบข้อถึก** — *"ถ้าเปลี่ยนตัวเลขในโจทย์ ต้องออกแรงเท่าเดิมไหม"* เปลี่ยนแล้วยังใช้
ไอเดียเดิมตอบได้เร็ว = ข้อดี / ต้องไล่ใหม่หมดด้วยแรงงานเท่าเดิม = ข้อถึก เขียนใหม่

โควตาต่อชุด 50 ข้อ (ปรับตามสัดส่วน): ไอเดียชี้ขาด (L3+) **≥ 25 ข้อ** / กลไกล้วน ≤ 15 ข้อ /
ไล่ค่าซ้ำ ๆ **≤ 2 ข้อทั้งชุด** — ตารางแนวที่ต้องการ-ต้องเลี่ยงแบบเต็ม และวิธีเพิ่มระดับ
โดยไม่เพิ่มความถึก: [DIFFICULTY.md](docs/DIFFICULTY.md)

> **อ่าน [CALIBRATION.md](docs/CALIBRATION.md) ก่อนคิดโจทย์ทุกครั้ง** — บัญชีว่าข้อสอบจริง
> ปี 66-68 ออกอะไรบ้างและยากได้แค่ไหน **ห้ามออกอัลกอริทึมที่มีชื่อ** (โจเซฟัส Boyer-Moore
> ฮัฟฟ์แมน นับการเปรียบเทียบของ sort/binary search ยุคลิดกรณีแย่สุด อินแวเรียนต์พาริตี)
> และฝั่งคณิต **ห้ามโจทย์ ม.ต้น** แนวอัตราเร็วเฉลี่ย ผสมสารละลาย อัตราส่วนแบ่งผลรวม
> — สองอย่างนี้คือสาเหตุที่ชุดที่ 5 ออกมายากเกินจริงและผิดแนวทั้งฉบับ

### ระดับความยาก

"ยาก" แปลว่า **มองไอเดียออกยาก** ไม่ใช่ **คำนวณเยอะ** ยากเพราะเลขเยอะ = ลดเลขลงแล้วเพิ่มชั้นการคิดแทน

| ระดับ | นิยาม | เวลา |
|---|---|---|
| L1 ง่ายมาก | แทนค่าลงสูตรที่โจทย์ให้ / อ่านค่าตรง ๆ 1 ขั้น | ≤ 1 นาที |
| L2 ง่าย | ขั้นตอนกลไกมาตรฐาน 2-3 ขั้น ไม่ต้องคิดวิธี | 1-2 นาที |
| L3 กลาง | ต้องมีข้อสังเกต 1 อย่างก่อน แล้วตามด้วย 2-3 ขั้น | 2-4 นาที |
| L4 ยาก | ต้องแบ่งกรณีเอง หรือคุมเงื่อนไขหลายชั้นให้ครบ | 4-7 นาที |
| L5 ยากมาก | เพดานบนสุด ข้อสอบจริงมีปีละ 0-1 ข้อ ใช้ให้น้อยที่สุด | 7-12 นาที |

**เส้นแบ่งที่ใช้จริง: L1-L2 = รู้วิธีก็ทำได้ / L3 ขึ้นไป = ต้องคิดวิธีเอง**
เขียนโจทย์เสร็จแล้ว **ประเมินระดับใหม่ทุกข้อ** แล้วบันทึกลง `ideas.json` คู่กับไอเดียชี้ขาด
preflight เอาไปเทียบพิมพ์เขียว เพี้ยนเกิน 20% **ให้แก้โจทย์ ไม่ใช่แก้พิมพ์เขียว**

```json
{"1": {"level": 3, "idea": "นับส่วนเติมเต็มแทนการนับตรง"},
 "2": {"level": 4, "idea": "...", "variant_of": "comp-set2#7"}}
```

**การกระจายคำตอบ** ก/ข/ค/ง ต้องอยู่ในช่วง 20-30% ต่อตัวเลือก **ห้ามเรียงตัวเลือกจากน้อย
ไปมากทุกข้อ** เพราะจะเดาตำแหน่งคำตอบได้ (ปี 68 ข้อ 1: 749, 901, 911, 897) preflight
ตรวจให้ทั้งสองอย่าง ข้ออัตนัยไม่นับรวม แต่คำตอบต้องไม่ซ้ำค่ากันทั้งตอน

## 3. ตรวจคำตอบและตัวลวง (ทำก่อนเขียน .tex เสมอ)

`verify.py` ต้องคำนวณ **สองอย่าง** ด้วยวิธีตรงไปตรงมาที่สุด (brute force ดีกว่าสูตรปิด เพราะกันพลาดจากการคิดสูตรผิด)

```python
R[37] = count_correct()                                       # คำตอบถูก
D[37] = {'ลืมกรณี n เป็นเลขคู่': count_odd_only(),             # ตัวลวง ทุกตัวมาจาก
         'นับลำดับด้วย (ใช้ P แทน C)': perm_version(),          # ความผิดที่ระบุชื่อได้
         'ตกเงื่อนไข n < 100': count_without_bound()}
emit(R, D, 'comp', 2)                                         # บรรทัดสุดท้าย → verify_out.json
```

1. **ตัวลวงทุกตัวต้องมาจาก `D[]`** ห้ามแต่งตัวเลขขึ้นเอง ถ้าคิดความผิดที่ทำให้ได้ตัวเลขนั้น
   ไม่ออก แปลว่าตัวลวงนั้นไม่สมจริง **ให้เปลี่ยนตัวลวง ไม่ใช่เปลี่ยนคำอธิบาย**
2. **ห้ามมีตัวลวงตัวไหนเท่ากับคำตอบถูก** — เท่าเมื่อไหร่คือข้อนั้นมีคำตอบถูก 2 ตัว
3. **ส่วน "ทำไมตัวเลือกอื่นผิด" ในเฉลย ต้องใช้ key ของ `D[]` ตรง ๆ**

โครงเต็ม ตัวอย่างทั้งสองพาร์ท และวิธีตั้งชื่อความผิดให้ใช้ได้จริง: [VERIFY.md](docs/VERIFY.md)

## 4. ไฟล์เฉลย

`answerN.tex` เป็น **ไฟล์แยกจากข้อสอบ** คอมไพล์แยก ส่งมอบเป็นคนละไฟล์ (`เฉลย_` นำหน้า)

**รูปแบบยึดไฟล์อ้างอิงสองไฟล์ของผู้ใช้** ใน `~/Documents/POSN.Computer/ข้อสอบเทียม/`
คือ `ตัวอย่างไฟล์เฉลย คอม.pdf` (30 ข้อ) และ `ตัวอ่างไฟล์เฉลย คณิต.pdf` (50 ข้อ)
ทั้งสองไฟล์ทุกข้อมีครบ 6 ส่วนและมีอย่างน้อย 3 ขั้น — ตัวเลขนี้นับจากไฟล์จริง ไม่ใช่ตั้งเอง

**โครงบังคับทุกข้อ — 6 ส่วน เรียงตามนี้** (macro อยู่ใน `preamble-answer.tex`)

| ส่วน | macro | เนื้อใน |
|---|---|---|
| คำตอบ | `\ansline{ค.\ $56$}{}` | ขึ้นหัวข้อ พาร์ทคอมใช้คำว่า "เฉลย" (`\renewcommand{\anskeyword}{เฉลย}`) |
| แนวคิด | `\idea{...}` | ข้อนี้วัดอะไร จุดชี้ขาดอยู่ตรงไหน ทำไมถึงเลือกวิธีนี้ |
| วิธีทำ | `\howto` + `\stepa{n}{...}` | **อย่างน้อย 3 ขั้น** ห้ามข้ามพีชคณิต |
| ตอบ | `\ansfinal{...}` | สรุปคำตอบซ้ำหลังจบวิธีทำ |
| ทำไมตัวเลือกอื่นผิด | `\whywrong{...}` | ขึ้นต้นด้วย `\quickck{...}` เสมอ แล้วไล่เหตุผลรายตัวเลือกจาก `D[]` |
| จุดพลาดที่พบบ่อย | `\pitfall{...}` | **ทุกข้อ ไม่ใช่เฉพาะข้อที่มีกับดัก** |

**ห้ามพิมพ์ `\textbf{แนวคิด}` เอง** ต้องใช้ macro เพราะ `preflight.py` นับ macro
เพื่อตรวจว่าครบ 6 ส่วน พิมพ์เองแล้วมันมองไม่เห็น เฉลยที่ขาดส่วนจะหลุดผ่านไป

- **`\quickck{}` คือวิธีตัดตัวเลือกทิ้งโดยไม่ต้องคำนวณซ้ำ** ไม่ใช่การทำโจทย์รอบสอง
  เช่น "ผลบวก 17 พจน์ต้องหารด้วย 17 ลงตัว" หรือ "ลูปซ้อนแบบนี้ต้องมากกว่าครึ่งของ $n^2$"
- **เลือกวิธีที่ง่ายที่สุดที่ทำได้จริงในห้องสอบ** ไม่มีเครื่องคิดเลข มีเวลาต่อข้อ ~3 นาที
  กรณีน้อยกว่า 10 ให้ไล่ตารางให้ดูเลย แล้วเอาสูตรไปไว้เป็นขั้นตรวจซ้ำท้ายข้อ
- **ห้ามข้ามพีชคณิต** จาก $3x+7=22$ ไป $x=5$ ต้องแสดง $3x=15$ ตรงกลาง
- **3 ขั้นคือพื้น ไม่ใช่เพดาน** ถ้าวิธีนั้นใช้ 5 ขั้นจริงก็เขียน 5 ขั้น แต่ห้ามยืดขั้นปลอม
  ให้ครบโควตา ถ้าเขียนได้ไม่ถึง 3 ขั้นแปลว่ายังย่อเกินไป กลับไปกางขั้นที่ข้ามให้ครบ
- เกณฑ์ตัดสิน: นักเรียนที่ทำข้อนั้นไม่ได้เลย ต้องอ่านเฉลยแล้วทำข้อคล้าย ๆ กันได้เอง
- แม่แบบเต็มข้อ: `assets/example-answer-math.tex` และ `assets/example-answer-comp.tex`
  เกณฑ์เลือกวิธีและกฎรายละเอียด: [ANSWER-KEY.md](docs/ANSWER-KEY.md)

## 5. ขนาดฟอนต์

`Scale = 1.112` ตั้งไว้ใน `assets/preamble-exam.tex` แล้ว **วัดมาแล้ว อย่าแก้**
ถ้าต้องปรับ (เช่นเทียบปีอื่น) ดู [FONT.md](docs/FONT.md)

## 6. ก่อนส่งมอบ

```bash
python3 ~/.claude/skills/posn/scripts/preflight.py <part> <set>    # รันในโฟลเดอร์งาน
```

ตรวจ 13 รายการในคำสั่งเดียว (verify + ตัวลวง + ตารางเฉลย + การกระจาย + การเรียงตัวเลือก +
ระดับเทียบพิมพ์เขียว + ขนาดฟอนต์ + log + ขอบเขตไพธอน + **เฉลยครบ 6 ส่วนและ >= 3 ขั้น** +
ไอเดียซ้ำ + เลขชุดไม่ทับบน Drive) **เจอ FAIL ข้อไหนมันหยุดทันที** แก้แล้วรันใหม่ ห้ามส่งมอบโดยที่ยังไม่ผ่านครบ

ท้าย output มันพิมพ์ **รายชื่อหน้าที่ต้องเปิดดูด้วยตา** (หน้าที่มี `\chstack` `pycode`
`tikzpicture` + หน้าแรก + หน้าสุดท้าย) — **ห้าม render หน้าอื่นโดยไม่มีเหตุผล** ชุด 50 ข้อ +
เฉลย 40 หน้าคือการอ่านภาพ 50+ ภาพเข้า context ซึ่งแพงที่สุดในไปป์ไลน์ และแพงโดยไม่ได้
อะไรเพิ่ม เพราะหน้าข้อความล้วนไม่เคยพัง

### Checklist ที่สคริปต์ตรวจแทนไม่ได้

- [ ] **สเปกตรงกับที่ผู้ใช้สั่ง** (พาร์ท/รูปแบบ/ความยาก/เรื่องที่เน้น/จำนวนข้อ/เลขชุด)
- [ ] **ทุกข้อผ่านบททดสอบ "เปลี่ยนตัวเลขแล้วต้องออกแรงเท่าเดิมไหม"** ข้อถึกต้องเขียนใหม่
- [ ] **สุ่มเปิดเฉลย 5 ข้อ ถามว่า "มีวิธีที่ง่ายกว่านี้ไหม"** และอ่านแล้วตามได้โดยไม่ต้องเติมขั้นเอง
- [ ] **ดูภาพหน้าที่ preflight ชี้ว่าเสี่ยง** ตัวเลือกไม่ทับกัน โจทย์ไม่ขาดหน้า การเยื้องของโค้ดถูก และข้อความล้วนที่สคริปต์ข้าม ไล่ตรวจด้วยตาแล้ว
- [ ] ไม่มีข้อไหนใช้เครื่องมือเกินขอบเขต ([SCOPE.md](docs/SCOPE.md)) · อัปขึ้น Drive ยืนยัน mimeType แล้ว
- [ ] **บันทึกดัชนีแล้ว** `exam_index.py add <part> <set> --ideas ideas.json`

## 7. ที่เก็บไฟล์

```
~/Documents/POSN.Computer/
├── ข้อสอบเทียม/{พาร์ทคณิตศาสตร์,พาร์ทคอมพิวเตอร์,ฉบับเต็ม}/
│       ข้อสอบเทียม_สอวนคอมพิวเตอร์_[พาร์ทคอม|ฉบับเต็ม]ชุดที่N.pdf   + เฉลย_ นำหน้า
├── ไฟล์ข้อสอบ/     ← ข้อสอบจริง อย่าแตะ      ├── blueprints/  ← <part>-set<N>.json
├── results/       ← ผลสอบ (/posn-score)     └── index.jsonl  ← ไอเดียชี้ขาดรายข้อ
```

ไม่มีช่องว่างหลัง "ชุดที่" (ชุดที่1 ไม่ใช่ ชุดที่ 1) `exam_blueprint.py` พิมพ์ path เต็มให้แล้ว
**เลขชุดนับแยกตามพาร์ท** และ **โฟลเดอร์ในเครื่องไม่ใช่บันทึกที่ครบ** ชุดที่ส่งมอบแล้วอยู่บน
Drive เสมอ แต่ในเครื่องอาจถูกลบ ตั้งเลขจากที่เห็นในเครื่องอย่างเดียว = ทับงานเก่า
ให้ `next_set.py` ตอบเท่านั้น (preflight ตรวจซ้ำอีกชั้น) · `.tex` `verify.py`
`ideas.json` เก็บใน scratchpad ของ session · `blueprints/` `results/` `index.jsonl`
เป็นข้อมูลส่วนตัวของผู้ใช้ **ห้าม commit ขึ้น repo**

## 8. อัปขึ้น Google Drive

```bash
~/.local/bin/rclone copy "<ไฟล์>" gdrive:POSN.Computer   # ห้ามใช้ sync — sync ลบไฟล์ปลายทาง
~/.local/bin/rclone ls gdrive:POSN.Computer              # ยืนยันขนาดตรงกับต้นทาง
```
อย่ากรอง output ของ rclone ทิ้งทั้งหมด ถ้าจะกรองให้เก็บ log ไว้ดูด้วย แล้วยืนยันซ้ำด้วย
`mcp__claude_ai_Google_Drive__search_files` ว่าได้ `application/pdf` ไม่ใช่
`application/vnd.google-apps.document` — วิธีที่ลองแล้วใช้ไม่ได้: [DRIVE.md](docs/DRIVE.md)

## หลังผู้ใช้ทำข้อสอบเสร็จ (คนละจังหวะกับการสร้าง)

`/posn-score` บันทึกผลสอบ (ถามแค่ 2 คำถาม) · `/posn-why` เจาะว่าข้อนั้นผิดเพราะอะไร ·
`/posn-weakness` รวมจุดอ่อนข้ามทุกชุด · `/posn-calibrate` ตรวจว่าป้ายความยากตรงความจริงไหม ·
`/posn-feynman` ให้ผู้ใช้อธิบายกลับเพื่อวัดว่าเฉลยดีพอไหม · `/posn-variant` ออกโจทย์ไอเดียเดิมหน้าตาใหม่

## เอกสารเพิ่มเติม

[SCOPE.md](docs/SCOPE.md) **ขอบเขตทางการ 2569** · [SPEC.md](docs/SPEC.md) สเปก·ปก·รหัสชุดวิชา · [DIFFICULTY.md](docs/DIFFICULTY.md) พรีเซ็ต·แนวที่ต้องเลี่ยง·วิธีเลื่อนระดับ ·
[COMPUTER-PART.md](docs/COMPUTER-PART.md) ขอบเขตไพธอน·โครง verify.py · [VERIFY.md](docs/VERIFY.md) `R[]` `D[]` ·
[ANSWER-KEY.md](docs/ANSWER-KEY.md) เฉลย 6 ส่วน·Feynman Mode · [SUBJECTIVE.md](docs/SUBJECTIVE.md) ตอนอัตนัย ·
[VARIANT.md](docs/VARIANT.md) โจทย์ไอเดียเดิม·เพดานลิขสิทธิ์ · [FONT.md](docs/FONT.md) ขนาดฟอนต์ ·
[DRIVE.md](docs/DRIVE.md) อัป Drive · [INSTALL.md](docs/INSTALL.md)·[SETUP.md](docs/SETUP.md) ย้ายเครื่อง ·
[TROUBLESHOOTING.md](docs/TROUBLESHOOTING.md) error ที่เคยเจอจริง

`scripts/` — `exam_blueprint.py` พิมพ์เขียว · `verify_lib.py` ตรวจตัวลวง · `check_answer_key.py` ตารางเฉลย ·
`measure_font_size.py` ขนาดฟอนต์ · `exam_index.py` ดัชนีโจทย์ · `exam_result.py` ผลสอบ · `next_set.py` เลขชุดว่าง · `preflight.py` รวมทุกการตรวจ
