% preamble ของไฟล์เฉลย — บรรทัดแรกของ answerN.tex เขียนว่า \input{preamble-answer}
% ต้องก๊อป preamble-exam.tex ไปวางในโฟลเดอร์เดียวกันด้วย เพราะไฟล์นี้ input ต่อจากมัน

\input{preamble-exam}
% ไม่คัดลอกซ้ำ — ฟอนต์ ขนาด geometry และ macro ตัวเลือก ก/ข/ค/ง มีที่อยู่ที่เดียว
% คือ preamble-exam.tex ถ้าแก้ Scale หรือ macro ตัวเลือก แก้ที่นั่นที่เดียวแล้วได้ทั้งสองไฟล์
% (ไฟล์เฉลยต้องใช้ macro ตัวเลือกด้วย เพราะยกตัวเลือกมาอ้างในส่วน "ทำไมตัวเลือกอื่นผิด")

\linespread{1.04}
% เฉลยแน่นกว่าข้อสอบเล็กน้อย เพราะยาว 20-45 หน้า และไม่มีช่องให้เขียนตอบ

% ---------------------------------------------------------------- macro เฉพาะเฉลย

\newcommand{\ansline}[2]{\par\noindent\textbf{\anskeyword}~~#1%
  \ifx\relax#2\relax\else~~\textit{(#2)}\fi\par\vspace{2pt}}
% อาร์กิวเมนต์แรกคือคำตอบ เขียนเป็น \ansline{ค.\ $17{,}010$}{} สำหรับข้อปรนัย
% และ \ansline{$45$}{} สำหรับข้ออัตนัย (ไม่มีตัวอักษรนำ)
%
% check_answer_key.py อ่านอาร์กิวเมนต์แรกของ macro นี้เพื่อเทียบกับตารางเฉลยและกับ
% ตัวเลือกจริงในข้อสอบ ดังนั้น **ห้ามเปลี่ยนชื่อ macro และห้ามสลับลำดับอาร์กิวเมนต์**
% อาร์กิวเมนต์ที่สองเป็นหมายเหตุสั้น ๆ ปล่อยว่างได้

\newcommand{\stepa}[2]{\par\vspace{3pt}\noindent\hspace*{1.2em}%
  \textbf{ขั้นที่ #1)}~~\parbox[t]{0.82\textwidth}{#2}\par\vspace{2pt}}
% ใช้ \parbox ไม่ใช่ itemize — \item ซ้อนทำให้ check_answer_key.py นับข้อเพี้ยน
% เพราะมันนับ \item ทั้งไฟล์ ดู docs/TROUBLESHOOTING.md

\newcommand{\keyhead}[1]{\par\vspace{8pt}\noindent\textbf{\large #1}\par
  \vspace{2pt}\hrule\vspace{6pt}}

% ตารางเฉลยหน้าแรก — แถวคู่ (ข้อ / ตอบ) เรียงต่อกันไป
% ข้อปรนัยใส่ตัวอักษร ก/ข/ค/ง ข้ออัตนัยใส่ตัวเลข check_answer_key.py รับทั้งสองแบบ
% หัวแถวต้องเขียนว่า \textbf{ตอบ} ตามด้วย & คั่นทุกช่อง สคริปต์หาแถวนี้ด้วยคำนั้น
%
% \begin{tabular}{|l|c|c|c|c|c|}\hline
% \textbf{ข้อ} & 1 & 2 & 3 & 4 & 5 \\\hline
% \textbf{ตอบ} & ก & ค & ข & ง & ค \\\hline
% \end{tabular}

% ใส่ \Needspace{4\baselineskip} ก่อน \item ทุกข้อในเฉลย (น้อยกว่าข้อสอบเพราะไม่มี
% ช่องตอบให้หลุดหน้า แต่ยังต้องกันหัวข้อขาดจากวิธีทำ)

% ---------------------------------------------------------------- โครง 6 ส่วนของแต่ละข้อ
%
% ทุกข้อต้องมีครบ 6 ส่วนตามลำดับนี้ — ถอดมาจากไฟล์อ้างอิงสองไฟล์ใน
% ~/Documents/POSN.Computer/ข้อสอบเทียม/ (ตัวอย่างไฟล์เฉลย คอม.pdf 30 ข้อ และ
% ตัวอ่างไฟล์เฉลย คณิต.pdf 50 ข้อ) ซึ่งนับแล้วมีครบทุกข้อ 100% ทั้งสองไฟล์
%
%   \ansline{...}{}   คำตอบบนหัวข้อ
%   \idea{...}        แนวคิด
%   \howto            หัวข้อ "วิธีทำ"
%   \stepa{1..n}{...} อย่างน้อย 3 ขั้นเสมอ (คณิต 50/50 ข้อ คอม 30/30 ข้อ มี >= 3 ขั้น)
%   \ansfinal{...}    ตอบซ้ำหลังจบวิธีทำ
%   \whywrong{...}    ทำไมตัวเลือกอื่นผิด — ขึ้นต้นด้วย \quickck{} เสมอ
%   \pitfall{...}     จุดพลาดที่พบบ่อย
%
% ใช้ macro พวกนี้แทนการพิมพ์ \textbf{แนวคิด} เอง เพราะ preflight.py ตรวจว่าครบ 6 ส่วน
% ด้วยการนับ macro ถ้าพิมพ์ \textbf{} เองมันจะมองไม่เห็นแล้วปล่อยเฉลยที่ขาดส่วนผ่านไป

\newcommand{\idea}[1]{\par\vspace{2pt}\noindent\textbf{แนวคิด}~~#1\par}
\newcommand{\howto}{\par\vspace{3pt}\noindent\textbf{วิธีทำ}\par}
\newcommand{\ansfinal}[1]{\par\vspace{3pt}\noindent\textbf{ตอบ}~~#1\par\vspace{2pt}}
\newcommand{\whywrong}[1]{\par\vspace{2pt}\noindent\textbf{ทำไมตัวเลือกอื่นผิด}~~#1\par}
\newcommand{\pitfall}[1]{\par\vspace{2pt}\noindent\textbf{จุดพลาดที่พบบ่อย}~~#1\par}
\newcommand{\wrongans}[1]{\par\vspace{2pt}\noindent\textbf{คำตอบที่มักเขียนผิด}~~#1\par}
% ข้ออัตนัยไม่มีตัวเลือกให้ตัด จึงใช้ \wrongans แทน \whywrong — ต้องมีอย่างน้อย 2 ค่า
% ที่คนทำจริงมักได้ ซึ่งก็คือ D[] ของข้อนั้น preflight.py รับทั้งสอง macro

\newcommand{\quickck}[1]{\textbf{ตัวตรวจเร็ว}~~#1\quad}
% วิธีตัดตัวเลือกทิ้งโดยไม่ต้องคำนวณซ้ำ — ของที่ใช้ได้จริงที่สุดในห้องสอบ
% ไฟล์อ้างอิงมีทุกข้อ (คณิต 50/50, คอม 29/30) จึงบังคับให้มีทุกข้อ
% วางเป็นตัวแรกใน \whywrong{} เสมอ แล้วค่อยไล่เหตุผลรายตัวเลือก

\newcommand{\qsep}[1]{\par\vspace{10pt}\hrule\vspace{6pt}%
  \noindent\textbf{\large ข้อ #1}\par\vspace{2pt}}
% หัวข้อแบบไฟล์เฉลยคณิต — เส้นคั่นเต็มบรรทัดแล้วตามด้วย "ข้อ N" ตัวหนาขนาดใหญ่
% ใช้กับเฉลยที่ไม่ได้ใส่ใน qlist (ไล่เลขข้อเอง) ถ้าใช้ qlist ให้ \item ตามปกติ

\newcommand{\anskeyword}{ตอบ}
% ป้ายหน้าคำตอบบนหัวข้อ — คณิตใช้ "ตอบ" (ค่าปริยาย) คอมใช้ "เฉลย"
% ไฟล์เฉลยพาร์ทคอมใส่ \renewcommand{\anskeyword}{เฉลย} ไว้หลัง \begin{document}
